auto-install-peers=false
strict-peer-dependencies=false
# Don't re-run an install check before `pnpm run <script>` (pnpm 11 feature).
# On CI this re-triggers the ignored-builds hard error for esbuild's postinstall
# and aborts otherwise-successful builds.
verify-deps-before-run=false
